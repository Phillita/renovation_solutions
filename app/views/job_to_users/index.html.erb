<h1>Listing job_to_users</h1>

<table>
  <tr>
    <th>Job</th>
    <th>User</th>
    <th></th>
    <th></th>
    <th></th>
  </tr>

<% @job_to_users.each do |job_to_user| %>
  <tr>
    <td><%= job_to_user.job_id %></td>
    <td><%= job_to_user.user_id %></td>
    <td><%= link_to 'Show', job_to_user %></td>
    <td><%= link_to 'Edit', edit_job_to_user_path(job_to_user) %></td>
    <td><%= link_to 'Destroy', job_to_user, :confirm => 'Are you sure?', :method => :delete %></td>
  </tr>
<% end %>
</table>

<br />

<%= link_to 'New Job to user', new_job_to_user_path %>
