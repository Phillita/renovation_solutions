<%= form_for(@job_to_user) do |f| %>
  <% if @job_to_user.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@job_to_user.errors.count, "error") %> prohibited this job_to_user from being saved:</h2>

      <ul>
      <% @job_to_user.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :job_id %><br />
    <%= f.text_field :job_id %>
  </div>
  <div class="field">
    <%= f.label :user_id %><br />
    <%= f.text_field :user_id %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
