<%= form_for(@task) do |f| %>
  <% if @task.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@task.errors.count, "error") %> prohibited this task from being saved:</h2>

      <ul>
      <% @task.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :job_id %><br />
    <%= f.text_field :job_id %>
  </div>
  <div class="field">
    <%= f.label :task_name %><br />
    <%= f.text_field :task_name %>
  </div>
  <div class="field">
    <%= f.label :task_description %><br />
    <%= f.text_field :task_description %>
  </div>
  <div class="field">
    <%= f.label :task_completed %><br />
    <%= f.date_select :task_completed %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
